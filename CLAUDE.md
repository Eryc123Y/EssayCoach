# CLAUDE.md (redirect)

Agent instructions, project status, commands, and conventions now live in `**AGENTS.md**` at the repository root.

Claude Code and other tools that still look for `CLAUDE.md` should treat `**AGENTS.md**` as the canonical file to read and update after significant changes.